<div id="main" class="mt-15 flex h-dvh w-full justify-center bg-main-bg pt-2">
  <div class="flex w-9/10 flex-col bg-white/50">
    <div id="profile-info" class="bord-1 flex w-full flex-col bg-white/75">
      <div class="flex w-full flex-col justify-center">
        <div class="profile-info">
          <%= current_user.email %></div>
        <div class="profile-info">
          <%= current_user.id %></div>
        <div class="profile-info">**************</div>
        <div class="profile-info">**************</div>
      </div>
      <div class="flex w-full justify-end gap-4">
        <span class="btn bg-blue-500/50 hover:bg-blue-500">Edit</span>
        <%= button_to "Cancel my account", registration_path(current_user), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete, class: "btn bg-red-500/50  hover:bg-red-500" %>
      </div>
    </div>
    <div>
      <div class="mt-5 flex bg-white/50">
        <span class="btn-profile">POSTS</span>
        <span class="btn-profile">COMMENT</span>
      </div>
    </div>
  </div>
</div>